#! /usr/bin/env ruby

$: << File.expand_path('../../lib', __FILE__)
require 'spinny'
require 'optparse'

options = {}
OptionParser.new do |opts|
  opts.banner = 'Usage: spinny-push DIR1 DIR2 ...'
end.parse!

dirs = ARGV.empty? ? STDIN.read.split("\n") : ARGV
Spinny::ListenClient.new(dirs).start!
